export interface MovingData {

    // позиция курсора относительно страницы при клике на слайдере (по оси Х)
    startPosition: number;

    // относительная позиция слайдера по оси Х
    tapePosition: number;
}